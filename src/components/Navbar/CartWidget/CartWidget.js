import React from 'react'

const CartWidget = (props) => {
    return (
        <div>
            {/* <img src="/image/ShopingCart.png"/> */}
            <p>{props.cart}</p>

        </div>
    )
}
  
export default CartWidget
